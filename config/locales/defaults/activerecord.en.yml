en:
  activerecord:
    errors:
      messages:
        record_invalid: 'Cannot save because of the following errors: %{errors}'
  errors:
    format: '%{attribute} %{message}'
    messages:
      blank: can't be blank
      inclusion: is not one of the options
      not_found: '%{model} not found'
      cant_delete: "%{model} can't be deleted"
      unauthorized: Unauthorized

